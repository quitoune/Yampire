{% extends 'base.html.twig' %}

{% block title %}{% trans %}photo.noms{% endtrans %}{% endblock %}

{% block body %}
    <div class="card text-center">
        <div class="card-header text-left">
            {{ 'photo.num_photo'|trans({'num': pagination.total }) }} - {{ pagination.page }} / {{ pagination.nbPages }}
        </div>
        <div class="card-body">
        	<div class="row">
        		{% for photo in photos %}
                    <div class="col-img">
						<a href="{{ path('photo_afficher', {'id':photo.id, 'page':pagination.page}) }}">
                            {{ photo(photo) | raw }}
                        </a>
                    </div>
                {% endfor %}
        	</div>
            {% if photos is empty %}
    	        <p class="text-info text-center">Il  n'y a aucune photo correspondant à la recherche.</p>
            {% endif %}
        </div>
        <div class="card-footer text-muted">
            {% include 'outils/pagination.html.twig' %}
        </div>
    </div>
{% endblock %}
